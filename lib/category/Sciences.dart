class Sciences {
  int category_id;
  String created_at;
  String icon;
  int id;
  String title;

  Sciences({required this.category_id, required this.created_at, required this.icon, required this.id, required this.title});
}